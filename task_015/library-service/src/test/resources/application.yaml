spring:
  datasource:
    url: jdbc:h2:mem:task_011_test
    username: sa
    password: 123
  liquibase:
    change-log: classpath:/liquibase/rootChangeLog.xml
  h2:
    console:
      enabled: true

security:
  enabled: true